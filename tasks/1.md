# Create project structure

Create a basic project structure with the following:
- A `src/` directory
- A `src/index.ts` file with a "Hello World" console.log
- A `package.json` with name "test-project" and version "1.0.0"
- A `tsconfig.json` with strict mode enabled

This is a test task to verify the orchestrator is working.