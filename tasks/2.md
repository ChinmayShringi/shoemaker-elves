# Add a utility function

In `src/utils.ts`, create a function called `greet(name: string): string` that returns `Hello, ${name}!`.

Then update `src/index.ts` to import and use this function, calling `greet("World")` and logging the result.